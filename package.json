{
  "name": "hearsay",
  "version": "0.1.0",
  "private": true,
  "description": "AI visibility tracker with Codex agent and Claude Code agent subscription measurements plus optional OpenAI, Anthropic, Gemini and Perplexity API measurements — self-hosted, zero dependencies, MCP built in.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "seed": "node scripts/seed.js",
    "run-panel": "node scripts/run-panel.js",
    "suggest-prompts": "node scripts/suggest-prompts.js",
    "typecheck": "tsc -p jsconfig.json --noEmit"
  }
}
