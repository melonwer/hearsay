/** Phase 2 (§15) — seeded DB end to end through the dashboard and API */

import test from 'node:test';

test('e2e.test.js', { skip: 'Phase 2 (§15) — seeded DB end to end through the dashboard and API' }, () => {});
