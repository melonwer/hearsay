/** Phase 1 Lane C (§10.1) — 404/405/415, 1MB cap, Origin, traversal, esc() */

import test from 'node:test';

test('router.test.js', { skip: 'Phase 1 Lane C (§10.1) — 404/405/415, 1MB cap, Origin, traversal, esc()' }, () => {});
