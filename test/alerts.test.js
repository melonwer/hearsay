/** Phase 1 Lane B (§9) — each rule fires and does not, 7-day dedup */

import test from 'node:test';

test('alerts.test.js', { skip: 'Phase 1 Lane B (§9) — each rule fires and does not, 7-day dedup' }, () => {});
