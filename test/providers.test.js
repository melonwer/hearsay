/** Phase 1 Lane A (§5) — fixture parse, error classification, retry once */

import test from 'node:test';

test('providers.test.js', { skip: 'Phase 1 Lane A (§5) — fixture parse, error classification, retry once' }, () => {});
