/** Phase 1 Lane B (§6.7) — pure post-processing, starter-pack fallback */

import test from 'node:test';

test('suggest.test.js', { skip: 'Phase 1 Lane B (§6.7) — pure post-processing, starter-pack fallback' }, () => {});
