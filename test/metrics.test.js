/** Phase 1 Lane B (§7) — Wilson anchor, SOV, trend gaps, branded exclusion */

import test from 'node:test';

test('metrics.test.js', { skip: 'Phase 1 Lane B (§7) — Wilson anchor, SOV, trend gaps, branded exclusion' }, () => {});
