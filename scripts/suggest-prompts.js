/**
 * CLI: node scripts/suggest-prompts.js "Brand" brand.com
 *
 * Drafts intents × paraphrases via core/suggest.js and prints them for review — it
 * never writes to the database (§6.7). Implemented in Phase 1 Lane B.
 */

process.stderr.write('scripts/suggest-prompts.js: not implemented yet (Phase 1 Lane B — §6.7).\n');
process.exitCode = 1;
