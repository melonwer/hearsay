/**
 * CLI: node scripts/screenshot.js — README screenshots (§18 step 4). Phase 3.
 *
 * Dev-only and deliberately not a dependency of the app: it drives Playwright from a
 * global/npx install at 1440×900 over the dashboard and answers pages in both themes.
 * Nothing in the shipped runtime imports this file.
 */

process.stderr.write('scripts/screenshot.js: not implemented yet (Phase 3 — §18).\n');
process.exitCode = 1;
