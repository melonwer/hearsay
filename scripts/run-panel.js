/**
 * CLI: node scripts/run-panel.js [--once] [--prompt "..."]
 *
 * Runs one panel through core/runner.js with trigger='manual'. Also the opt-in live
 * smoke path for humans (HEARSAY_LIVE_TEST=1); tests never hit the network (§15).
 * Implemented in Phase 1 Lane A.
 */

process.stderr.write('scripts/run-panel.js: not implemented yet (Phase 1 Lane A — §8.1).\n');
process.exitCode = 1;
